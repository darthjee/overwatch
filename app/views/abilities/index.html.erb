<div ng-hide="controller.loaded">Loading ...</div>

<div ng-show="controller.loaded">
  <div class="col-md-4 col-sm-12">
    <div class="row">

      <table class="table">
        <tr>
          <th>#</th>
          <% %w(name description ultimate? actions).each do |key| %>
            <th><%= as_title(key) %></th>
          <% end %>
        </tr>
        <tr ng-repeat="ability in controller.data">
          <td><a href="#/abilities/{{ ability.id }}">{{ ability.id }}</a></td>
          <% %w(name description is_ultimate).each do |key| %>
            <td>{{ ability.<%= key %> }}</td>
          <% end %>
          <td class="btn-group">
            <a class="btn btn-default" href='<%=
              angular_path_to(edit_ability_safe_path(id: '{{ ability.id }}'))
            %>'>Edit</a>
            <a class="btn btn-default" ng-click="controller.delete(hero.id)">Delete</a>
          </td>
        </tr>
      <table>

      <div class="col-sm-12">
        <div class="row">
          <a class="btn btn-default" href='<%= angular_path_to(new_ability_path) %>'>New</a>
        </div>
      </div>

    </div>
  </div>
</div>
